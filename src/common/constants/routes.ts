const baseUrl = '/';

export const routes = {
  default: `${baseUrl}`,
  home: `${baseUrl}home`,
  mapPageLayout: `${baseUrl}mapPageLayout`,
  spainMunicipalitiesElectoralMap2016: `${baseUrl}mapa-electoral-municipios-espana-2016`,
};
